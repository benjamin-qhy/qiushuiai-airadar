# qiushuiai-airadar MVP 最终验收报告

> 状态：本次 MVP 验收通过。macOS、真实数据与 Windows Service 自动验收均已通过；用户于 2026-09-14 明确批准将 Windows 实机重启和 Tailscale 手机访问延期到 Issue #37 执行。这两项尚未实测，不得把本次延期记录当作实测证据。

## 验收环境

- 日期：2026-09-14
- Node.js：24.21.0
- pnpm：10.34.1
- macOS：26.5.2，Apple Silicon
- 验收数据目录：`/Users/qiushui/.airadar/acceptance/2026-09-14-mvp`
- 数据目录为本次新建目录，验收结束后保留。

## 真实内容结果

- 真实内容：76 条
- 发现记录：83 条
- 内容形态：视频 48、文章 10、短内容 15、图文 2；另有 1 条尚未确定形态的记录
- 六类信源：RSS、X、YouTube、抖音、视频号、小红书均已跑通
- 真实供应商返回：TwitterAPI.io 10 条、TikHub X 10 条、Google YouTube 10 条、TikHub YouTube 10 条、TikHub 抖音 5 条、Get笔记抖音 5 条、TikHub 视频号 5 条、TikHub 小红书 1 条
- 跨来源去重：同一 YouTube 内容经 3 次发现后只保留 1 条内容记录，并保留 3 份互动快照
- 互动快照：85 份
- 增量断点：抖音、Get笔记、视频号、小红书均生成并复验游标
- 严格补全：抖音正文、视频号 4 张关键帧、小红书 5 张图片均成功；缺图片、缺转写、单条失败隔离的负向门禁均通过
- 故障切换：X 的主供应商凭据失败后切换 TikHub 成功；YouTube 的主供应商请求失败后切换 TikHub 成功

验收库中有 21 个任务，其中 9 个成功、12 个失败。12 个失败任务来自故意触发的严格补全和故障隔离负向验收，不是未恢复的正式采集故障。

## 模型 Token 与费用

- 模型：`gpt-5.3-codex-spark`（真实验收的唯一模型）
- 分析记录：6 条；模型调用审计：33 条
- 输入 Token：7,976
- 输出 Token：4,975
- 缓存读取/写入 Token：0 / 0
- 总费用：USD 0.083608

## 操作、版本与回退

- 真实批量操作：2 条内容批量标记已读成功，随后恢复到原状态
- 参数版本：批量上限从 100 改为 99 后，新版本立即生效；回退后恢复为 100
- 人工操作状态、参数版本和审计记录均保存在验收数据目录

## 安装、升级与恢复

### macOS

- 从提交 `5115496` 生成独立 tarball 并安装到 `~/.airadar/program`，不是从仓库直接运行
- launchd 定义：`~/Library/LaunchAgents/ai.qiushuiai.airadar.plist`
- 安装、启动、停止、重新启动、开机启动配置全部通过
- 最终包异常恢复：强制结束 PID 49495 后，launchd 自动拉起 PID 49545，健康接口继续返回 ready
- launchd 实际指向安装后的 CLI，固定使用 Node.js 24.21.0，并读取验收目录中的 76 条真实内容
- 安装后 CLI SHA-256：`f9febefa54ebd383d112ce4673759501ec4205fb5eaaf023af188c069d1c70fa`

### Windows

- 安装包会下载固定 WinSW 2.12.0，并验证 SHA-256 后注册 `QiushuiAIAIRadar` Windows Service
- 服务明确使用 LocalSystem，并持久化用户的 `CODEX_AUTH_PATH`；Tailscale Origin 和外部转写地址也会随服务定义保存
- 服务配置为 Automatic，并设置 5 秒、15 秒两级异常重启
- PR #36 的 [Windows 工作流 #34849366374](https://github.com/benjamin-qhy/qiushuiai-airadar/actions/runs/34849366374) 已在提交 `217c434` 的真实 `windows-latest` 上通过
- 实际验证：安装、Automatic 启动类型、启动、停止、重启、Web 健康检查、旧版升级后数据不变及卸载全部通过
- 异常恢复：强制结束 PID 2272 后，Windows Service 自动拉起 PID 6748，健康检查继续通过
- Windows 真正重启后的自动启动：本次由用户批准延期，后续在 Issue #37 补做并记录真实重启证据
- Windows 通过 Tailscale 使用手机访问：本次由用户批准延期，后续在 Issue #37 补做并保存手机截图

本次交付按用户决定将上述两项从 Issue #22 的当前合并门槛中移出，但未取消实际验收义务；后续结果必须回填本报告。

程序升级 smoke 会从固定的上一版本提交 `ae6d470` 构建并启动真实服务、生成 36 份权威信源记录，再升级到 0.1.0 正式独立包；新版本会恢复并读取旧数据，同时验证业务数据目录中的标记文件内容完全不变。程序目录与数据目录分离。

## 页面速度与截图

在 76 条真实内容下，所有常用页面和 API 均低于 2 秒目标：

| 路径 | HTTP | 用时（秒） |
| --- | ---: | ---: |
| `/` | 200 | 0.001874 |
| `/daily` | 200 | 0.000740 |
| `/all` | 200 | 0.000652 |
| `/sources` | 200 | 0.000527 |
| `/runtime` | 200 | 0.000624 |
| `/config` | 200 | 0.000497 |
| `/api/daily` | 200 | 0.016007 |
| `/api/contents` | 200 | 0.009056 |
| `/api/sources` | 200 | 0.001773 |
| `/api/runtime` | 200 | 0.000826 |
| `/api/config` | 200 | 0.000787 |

- macOS 桌面真实数据：[截图](../../design-evidence/issue-22-macos-real-data.png)
- 390×844 移动端真实数据：[截图](../../design-evidence/issue-22-mobile-real-data.png)
- 移动端实测 `innerWidth = 390`、`scrollWidth = 390`，无横向溢出

## 密钥扫描

自动扫描将 5 个真实密钥逐一与以下输出做字节比对：

- 452 个业务数据、日志和仓库 Markdown 文件
- 18 个 Markdown
- 2 个 SQLite 文件
- 2 个服务日志
- 6 个前端 API 响应

结果：通过，未发现真实密钥进入 Markdown、SQLite、日志或前端接口。正式密钥文件权限为 `600`。

## 前后端会话数据边界

本项目 Web 不承载 assistant-ui 会话，也没有 `ExternalStoreRuntime` 会话数据；前端只消费后台服务提供的 REST DTO，权威业务状态始终由后台 `RuntimeRepository` 管理。因此本工单不存在把后端会话对象直接放入前端 Runtime 的例外，也不影响最终交付。
