# qiushuiai-airadar 本地开发、远程发布、安装与更新

适用版本：0.5.0 及以后。最后验证：2026-09-19，macOS。

开发版用于修改和测试程序，安装版用于日常正式使用。两者端口和数据目录完全分开：

| 用途   | 页面                     | 接口    | 数据目录                                |
| ------ | ------------------------ | ------- | --------------------------------------- |
| 开发版 | `http://127.0.0.1:5173`  | `43111` | `~/.qiushuiai-airadar/development-data` |
| 安装版 | `http://127.0.0.1:43120` | `43120` | `~/.qiushuiai-airadar/production-data`  |

程序安装在 `~/.qiushuiai-airadar/program`，后台日志位于 `~/.qiushuiai-airadar/service/logs`，更新备份位于 `~/.qiushuiai-airadar/backups`。修改源码不会自动改变安装版，开发数据也不会进入正式数据。

## 1. 环境准备

开发需要 Node.js 24.21.0 或更高的 24.x，以及 pnpm 10.34.1。安装和更新需要 Node.js 24.x、npm 和 curl。

这台 Mac 每次打开新终端后可执行：

```bash
export PATH="/opt/homebrew/opt/node@24/bin:$HOME/.qiushuiai-airadar/program/bin:$PATH"
node -v
```

应显示 `v24.21.0` 或更高的 24.x。需要长期生效时，把 export 命令加入 `~/.zshrc`。所有操作都不需要 `sudo`。

## 2. 本地开发

```bash
cd /Users/qiushui/work/qiushui/qiushuiai-airadar
pnpm install --frozen-lockfile
```

开两个终端：

```bash
# 终端一：开发接口 43111，读写 development-data
pnpm dev:service
```

```bash
# 终端二：开发网页 5173
pnpm dev
```

打开 `http://127.0.0.1:5173`。网页代码通常会自动刷新；后端代码修改后需重启开发接口。

端口被占用时，可临时使用另一组开发端口：

```bash
QIUSHUIAI_AIRADAR_DEV_PORT=43112 pnpm dev:service
QIUSHUIAI_AIRADAR_API_URL=http://127.0.0.1:43112 pnpm --filter @qiushuiai-airadar/web dev --port 5174
```

不要把 `QIUSHUIAI_AIRADAR_DATA_ROOT` 指向 `production-data` 做开发。

## 3. 生成本地发布包

先修改根目录和 `apps/cli/package.json` 中的版本号，再执行：

```bash
pnpm check
pnpm release:local
```

以 0.5.0 为例，会生成：

```text
release/qiushuiai-airadar-cli-0.5.0.tgz
release/qiushuiai-airadar-cli-0.5.0.tgz.sha256
release/qiushuiai-airadar-release.json
```

安装包包含程序、网页、默认 YAML 配置和提示词，不包含用户数据与密钥。同一版本不能重复生成，下一次发布前必须增加版本号。

## 4. 发布远程版本

远程版本发布到 [GitHub Releases](https://github.com/benjamin-qhy/qiushuiai-airadar/releases)。需要先安装并登录 GitHub CLI。

```bash
# 检查并打包
pnpm check
pnpm release:local

# 提交并推送
git status
git add <本版本文件>
git commit -m "发布 qiushuiai-airadar 0.5.0"
git push origin main

# 创建远程 Release
pnpm release:remote
gh release view v0.5.0
```

`release:remote` 只允许从干净工作区发布，并要求当前提交已推送到 `origin/main`。每个 Release 包含安装包、SHA-256 校验文件、`qiushuiai-airadar-release.json` 和 `install-release.sh`。安装与升级读取静态清单，不受 GitHub API 匿名限额影响。

## 5. 从远程首次安装

```bash
installer="$(mktemp /tmp/qiushuiai-airadar-install.XXXXXX)"
curl -fsSL \
  https://github.com/benjamin-qhy/qiushuiai-airadar/releases/latest/download/install-release.sh \
  -o "$installer"
less "$installer"
sh "$installer"
rm "$installer"
```

脚本会先下载并校验文件，然后备份正式数据、安装程序、注册 macOS 后台服务，并检查版本与健康状态。完成后打开 `http://127.0.0.1:43120`。

首次安装需要联网获取安装包中的 npm 依赖。浏览器关闭后服务仍会运行，用户登录 macOS 后自动启动。

## 6. 旧目录自动迁移

0.4.0 把原来的 `~/.airadar` 整体迁移为：

```text
~/.qiushuiai-airadar
```

远程安装脚本会按以下规则迁移：

1. 先下载并校验新版本，确认文件可用后才停止旧服务。
2. 如果旧目录与新目录同时存在，立即停止，不合并、不覆盖。
3. 将整个旧目录改名为 `~/.qiushuiai-airadar`。
4. 当前开发数据 `single-table-data` 改名为 `development-data`。
5. 正式数据 `installed-data` 改名为 `production-data`。
6. 旧数据库文件改为 `qiushuiai-airadar.sqlite`，数据库内容不变。
7. 当前安装程序和日志分别改为 `program` 与 `service`。
8. 同时存在的更早开发数据或程序目录移入 `backups` 保留。

迁移只改路径，不清空数据库、Markdown、配置、密钥或历史备份。

## 7. 从远程升级

安装 0.4.0 后执行：

```bash
qiushuiai-airadar upgrade
```

发现新版本后，程序会下载并校验安装包，把 `production-data` 备份到 `backups`，停止正式服务，替换 `program` 中的程序，再启动并核对版本。已经是最新版时不会重复安装。

需要重新安装当前版本时：

```bash
qiushuiai-airadar upgrade --force
```

升级后检查：

```bash
qiushuiai-airadar --version
qiushuiai-airadar service status
curl --noproxy '*' -fsS http://127.0.0.1:43120/health
```

应看到当前版本、`running` 和 `"status":"ready"`。更新只替换程序；正式内容、已读、收藏、YAML 配置、信源状态和 `.env` 都保留在 `production-data`。

## 8. 第一次配置和采集

新安装没有内容是正常的：

1. 打开“信源管理”，点击“新增信源”，填写平台、名称、账号或订阅地址后保存。
2. 信源可以随时编辑、启用、停用和删除；删除信源不会删除已经采集的内容。
3. 在同一页面的“平台与供应商”区域填写密钥，点击“设为首选并保存”，再点击“测试连接”。密钥只保存在本机，不会在页面或接口中回显。
4. 打开“系统配置”，直接修改 YAML，点击“校验并保存”。格式或内容不合法时不会覆盖原文件。
5. 执行一次手动采集并刷新页面。

```bash
chmod 600 "$HOME/.qiushuiai-airadar/production-data/.env"
qiushuiai-airadar collect
```

也可以直接编辑 `~/.qiushuiai-airadar/production-data/.env`，按需填写 `TIKHUB_API_KEY`、`TWITTERAPI_IO_KEY` 和 `YOUTUBE_API_KEY`，然后执行 `chmod 600`。开发环境页面修改的是 `development-data`，正式安装页面修改的是 `production-data`，两套数据互不影响。AI 分析默认读取 `~/.codex/auth.json`。当前没有自动定时采集和自动定时更新。

## 9. 日常管理

```bash
qiushuiai-airadar --version
qiushuiai-airadar service status
qiushuiai-airadar service stop
qiushuiai-airadar service start
qiushuiai-airadar service restart
qiushuiai-airadar collect
qiushuiai-airadar upgrade
qiushuiai-airadar service uninstall
```

| 内容               | 默认位置                                                      |
| ------------------ | ------------------------------------------------------------- |
| 开发数据           | `~/.qiushuiai-airadar/development-data`                       |
| 正式数据           | `~/.qiushuiai-airadar/production-data`                        |
| 用户配置           | `~/.qiushuiai-airadar/production-data/config`                 |
| 密钥               | `~/.qiushuiai-airadar/production-data/.env`                   |
| 安装程序           | `~/.qiushuiai-airadar/program`                                |
| 服务日志           | `~/.qiushuiai-airadar/service/logs`                           |
| 更新与迁移备份     | `~/.qiushuiai-airadar/backups`                                |
| macOS 正式服务定义 | `~/Library/LaunchAgents/ai.qiushuiai.qiushuiai-airadar.plist` |

查看最近错误：

```bash
tail -n 50 "$HOME/.qiushuiai-airadar/service/logs/service.error.log"
```

升级失败时不要删除数据。先检查 `backups` 和日志，再决定是否执行 `qiushuiai-airadar upgrade --force`。

## 10. 0.5.0 实机验收

- 完整执行代码检查、类型检查、单元测试、构建与独立安装测试。
- 开发页面 5173、开发接口 43111、正式服务 43120 同时运行。
- 从 `~/.airadar` 迁移到新目录，开发数据与正式数据分开保留。
- 真实创建 GitHub Release，并从远程下载安装。
- 执行 `upgrade` 的最新版判断与 `upgrade --force` 重新安装。
- 核对版本、后台状态、健康接口、数据目录和升级备份。

Windows 服务管理代码仍保留；本次远程发布、目录迁移与升级只在这台 Mac 上完成实机验收。
