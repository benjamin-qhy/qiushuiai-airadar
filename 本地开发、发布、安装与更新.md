# AI Radar 本地开发、远程发布、安装与更新

适用版本：0.3.0 及以后。最后验证：2026-09-19，macOS。

开发版和安装版是两个相互独立的环境：开发版用于修改程序，安装版用于日常使用。它们使用不同端口和数据目录，修改源码不会自动改变安装版。

| 用途   | 页面                     | 接口    | 数据目录                              |
| ------ | ------------------------ | ------- | ------------------------------------- |
| 开发版 | `http://127.0.0.1:5173`  | `43111` | `~/.qiushuiai-airadar/dev-data`       |
| 安装版 | `http://127.0.0.1:43120` | `43120` | `~/.qiushuiai-airadar/installed-data` |

`~` 表示当前用户目录，本机是 `/Users/qiushui`。安装版只监听本机地址，外部电脑无法直接访问。

## 1. 环境准备

开发需要 Node.js 24.21.0 或更高的 24.x，以及 pnpm 10.34.1。安装和更新需要 Node.js 24.x、npm 和 curl。

这台 Mac 的 Node.js 24 位于 Homebrew 目录。每次打开新终端先执行：

```bash
export PATH="/opt/homebrew/opt/node@24/bin:$HOME/.qiushuiai-airadar/installed-program/bin:$PATH"
node -v
```

应显示 `v24.21.0` 或更高的 24.x。需要长期生效时，把 `export PATH=...` 加入 `~/.zshrc`。本文所有操作都不需要 `sudo`。

## 2. 本地开发

```bash
cd /Users/qiushui/work/qiushui/qiushuiai-airadar
pnpm install --frozen-lockfile
```

开两个终端分别运行：

```bash
# 终端一：开发接口 43111
pnpm dev:service
```

```bash
# 终端二：开发网页 5173
pnpm dev
```

打开 `http://127.0.0.1:5173`。网页源码修改后会自动刷新；后端修改后需停止并重新运行接口。停止自己启动的服务用 `Ctrl+C`。

端口已被其他开发实例占用时，可以临时使用另一组端口：

```bash
AIRADAR_DEV_PORT=43112 pnpm dev:service
AIRADAR_API_URL=http://127.0.0.1:43112 pnpm --filter @airadar/web dev --port 5174
```

此时打开 `http://127.0.0.1:5174`。开发版与安装版不要指定到同一数据目录。

## 3. 生成本地安装包

修改完成后先运行完整检查：

```bash
pnpm check
pnpm release:local
```

以 0.3.0 为例，会生成：

```text
release/airadar-cli-0.3.0.tgz
release/airadar-cli-0.3.0.tgz.sha256
```

`.tgz` 是安装包，`.sha256` 用于确认文件没有损坏。包内包含程序、网页、默认 YAML 配置和提示词，不包含用户内容与密钥。同一版本不会覆盖已有安装包；发布下一版前必须先修改根目录和 `apps/cli` 中的版本号。

例如准备 0.3.1：

```bash
npm pkg set version=0.3.1
npm pkg set version=0.3.1 --prefix apps/cli
pnpm check
pnpm release:local
```

## 4. 发布到 GitHub Releases

远程版本发布到 [benjamin-qhy/qiushuiai-airadar Releases](https://github.com/benjamin-qhy/qiushuiai-airadar/releases)。发布前需要安装并登录 GitHub CLI，且代码必须已提交、推送，当前 `HEAD` 与 `origin/main` 完全一致。

发布顺序：

```bash
# 1. 完整检查并生成本地安装包
pnpm check
pnpm release:local

# 2. 检查改动，提交并推送
git status
git add <本版本相关文件>
git commit -m "发布 AI Radar 0.3.1"
git push origin main

# 3. 创建远程 Release 并上传安装包、校验文件和安装脚本
pnpm release:remote
```

远程发布脚本会拒绝以下情况：工作区有未提交改动、当前提交尚未推送、安装包缺失，或同名 Release 已存在。发布成功后检查：

```bash
gh release view v0.3.1
```

远程 Release 包含三个文件：安装包、SHA-256 校验文件和 `install-release.sh`。Release 标签固定为 `v版本号`，例如 `v0.3.1`。

## 5. 从远程首次安装

先下载脚本到本地再执行，便于运行前查看内容：

```bash
installer="$(mktemp /tmp/qiushuiai-airadar-install.XXXXXX)"
curl -fsSL \
  https://github.com/benjamin-qhy/qiushuiai-airadar/releases/latest/download/install-release.sh \
  -o "$installer"
less "$installer"
sh "$installer"
rm "$installer"
```

脚本会自动完成：读取远程最新版、下载安装包和校验文件、核对 SHA-256、备份已有安装数据、安装程序、注册 macOS 后台服务、检查版本和健康状态。完成后打开 `http://127.0.0.1:43120`。

首次安装需要联网下载 npm 依赖，因此当前安装包不是完全离线包。浏览器关闭后后台服务仍会运行，用户登录 macOS 后会自动启动。

## 6. 从旧的 `.airadar` 迁移

0.3.0 将应用目录统一改为：

```text
~/.qiushuiai-airadar
```

从 0.2.1 或更早版本第一次执行远程安装脚本时，脚本会：

1. 停止旧安装服务和旧开发后台服务。
2. 确认 `~/.airadar` 与 `~/.qiushuiai-airadar` 没有同时存在。
3. 将整个 `~/.airadar` 改名为 `~/.qiushuiai-airadar`，内容、数据库、配置、密钥、日志和历史备份一起保留。
4. 在新目录安装程序并重新注册安装版后台服务。

如果两个目录同时存在，脚本会停止迁移并要求人工确认，不会合并或覆盖。迁移成功后不再使用 `~/.airadar`。项目里自行维护的开发后台服务需要把 plist 中的旧路径改成新路径后重新启动。

## 7. 本地通过远程版本升级

0.3.0 及以后直接执行：

```bash
airadar upgrade
```

升级命令会查询 GitHub 最新正式 Release。发现新版本后，它会依次执行：

1. 下载 `.tgz` 和 `.sha256`。
2. 校验安装包。
3. 将 `installed-data` 备份到 `~/.qiushuiai-airadar/backups/`。
4. 停止后台服务。
5. 替换 `installed-program` 中的程序。
6. 重新注册并启动后台服务。
7. 校验安装版本。

已经是最新版时不会重复安装。需要重新安装当前版本用于修复程序文件时使用：

```bash
airadar upgrade --force
```

升级完成后检查：

```bash
airadar --version
airadar service status
curl --noproxy '*' -fsS http://127.0.0.1:43120/health
```

应看到新版本、`running` 和 `"status":"ready"`。`airadar status` 只检查命令本身，不能替代服务健康检查。

更新只替换程序；内容、已读、收藏、YAML 配置、信源状态和 `.env` 都保存在 `installed-data` 中。已有配置不会被默认配置覆盖。未来版本若改变存储格式，应先阅读对应 Release 说明。

## 8. 第一次配置和采集

新安装没有内容是正常的：

1. 打开“信源管理”，启用需要的信源。
2. 在 `~/.qiushuiai-airadar/installed-data/.env` 填入已有供应商密钥，并把权限设为 `600`。
3. 在“系统配置”查看 `~/.qiushuiai-airadar/installed-data/config/` 中的配置。
4. 运行 `airadar collect`，完成后刷新页面。

```bash
chmod 600 "$HOME/.qiushuiai-airadar/installed-data/.env"
airadar collect
```

可按需填写 `TIKHUB_API_KEY`、`TWITTERAPI_IO_KEY` 和 `YOUTUBE_API_KEY`。AI 分析默认读取 `~/.codex/auth.json`；需要指定其他已有登录文件时，在 `.env` 填 `CODEX_AUTH_PATH=/绝对路径/auth.json`。

`collect` 会调用已启用信源和模型，可能消耗供应商额度。一次只运行一个采集命令。当前没有自动定时采集和自动定时更新；`runtime.yaml` 中的排程字段不会让服务自行采集。

## 9. 日常管理与故障恢复

```bash
airadar --version          # 查看版本
airadar service status     # 查看后台状态
airadar service stop       # 停止服务
airadar service start      # 启动服务
airadar service restart    # 重启服务
airadar collect            # 手动采集一轮
airadar upgrade            # 从 GitHub 升级
airadar service uninstall  # 移除后台服务，保留程序和数据
```

| 内容                  | 默认位置                                                      |
| --------------------- | ------------------------------------------------------------- |
| 安装程序              | `~/.qiushuiai-airadar/installed-program`                      |
| 内容数据库和 Markdown | `~/.qiushuiai-airadar/installed-data`                         |
| 用户配置              | `~/.qiushuiai-airadar/installed-data/config`                  |
| 密钥                  | `~/.qiushuiai-airadar/installed-data/.env`                    |
| 服务日志              | `~/.qiushuiai-airadar/installed-service/logs`                 |
| 更新备份              | `~/.qiushuiai-airadar/backups`                                |
| macOS 后台定义        | `~/Library/LaunchAgents/ai.qiushuiai.airadar-installed.plist` |

查看最近错误：

```bash
tail -n 50 "$HOME/.qiushuiai-airadar/installed-service/logs/service.error.log"
```

升级失败时不要删除数据。先检查备份和日志，再决定是否重新执行 `airadar upgrade --force`。需要退回旧版时，从对应 GitHub Release 下载旧 `.tgz` 和校验文件，校验后安装旧程序；若该版本改变过存储格式，还要按该版本说明恢复升级前备份。

## 10. 0.3.0 验收范围

- 本地代码检查、类型检查、单元测试、构建和独立安装测试。
- 开发端口与安装端口同时运行，互不占用。
- 本地安装包校验、网页静态文件、信源接口、内容接口和手动采集命令。
- 从旧目录整体迁移到 `~/.qiushuiai-airadar`，原数据和配置保留。
- GitHub Release 真实发布及远程资产下载。
- 本机从远程 Release 安装，并通过 `airadar upgrade --force` 重新下载安装当前远程版本。
- 升级后的后台服务、网页、版本、健康状态和备份验证。

Windows 服务管理代码仍保留，但本次远程发布、目录迁移和升级流程只在这台 Mac 上完成实机验收。
